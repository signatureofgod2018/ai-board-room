{
  "name": "@basilica/cli",
  "version": "0.1.0",
  "description": "basilica CLI — capture, import, checkpoint, export, list, search",
  "type": "module",
  "bin": {
    "basilica": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@basilica/core": "*",
    "@basilica/openclaw": "*",
    "@basilica/storage": "*",
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
